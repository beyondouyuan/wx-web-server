import React, { Component } from 'react';
import render from '../render';

import './style.scss';

class Constitution extends Component {
    render() {
        return(
            <div>
                章程
            </div>
        )
    }
}

render(Constitution)